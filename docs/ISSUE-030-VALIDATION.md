# #30 设置与按需适配

## 2026-09-06 本轮范围

- 设置组件的旧 CSS 保留 sticky/bottom/z-index，遮挡宿主滚动表单。改为正常流、透明背景；真实 rc2 WebUI 已检查单模型、风险分级、高级参数展开。截图来自实际服务，不是模拟页。测试选择未保存，刷新后回到原配置。
- 255 个 Vitest 测试、类型检查通过；安装器 13 项 Node 测试通过。
- 实际 tgz 在 DSH 0.1.0-rc.6、0.1.1-rc.2、0.1.2-alpha.5 上使用官方 plugin add，完成匹配下载、哈希核验、幂等安装、原文件还原、plugin remove。
- rc2 额外先安装 0.6.0 的 CI 制品再升级到 0.6.1，确认仅一个生效 bundle、旧三套 UI 目录移除，非插件测试标记保留。升级由官方安装器执行，不另行扫描/删除用户数据。
- 首轮远程 GitHub raw 直连失败；使用显式 HTTPS 镜像重测成功。错误发生在替换前，没有修改宿主 owner。

## 边界

本轮没有重新运行长时间模型任务，没有重新认证原生沙盒或 reviewer 的安全判断。下载的版本专属 owner 来自 v0.6.0 冻结制品，逐文件 SHA-256 校验；设置组件来自新主包。主包共用后端不是另外三个后端包。未知 DSH 版本仍拒绝安装。

当前测试环境未发现其他重复安装的插件包。bridge host/platform 是依赖，不作为残留删除。npm 缓存和其他 profile 不做全局清理，保留原 UI 备份以支持恢复。

README 的约 8 次/数万输入 token/约 50% 缓存来自此前反馈的小样本示例，不是本次测量或费用保证。
